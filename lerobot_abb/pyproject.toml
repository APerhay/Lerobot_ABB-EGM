[project]
name = "lerobot-abb"
version = "0.1.0"
dependencies = [
    "lerobot",           # Ensures lerobot is installed
    "ABBRobotEGM",      # The library from the repo you mentioned
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"